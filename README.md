## GoodPeers 
### Usage 
```bash
go build .
 ./good-peers -h
  -ms int
        maximum time(ms) (default 1000)
  -n int
        number of peers (default 30)
  -rpc string
        rpc url (default "https://sei-rpc.polkachu.com")
```
